# To create and activate, run:
#     python3 -m venv env
#     source env/bin/activate
# To install with dependencies, run:
#     python3 -m pip install -r requirements.txt -e .
# The `-e .` is for editable mode. See README.md for more.
numpy
netcdf4
dask
xarray
